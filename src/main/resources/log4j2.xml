<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="ERROR">

    <Appenders>
        <Console name="LogToConsole" target="SYSTEM_OUT">
            <PatternLayout
                pattern="%d [%t] - %highlight{%p}{FATAL=red blink, ERROR=red, WARN=yellow bold, INFO=blue bold, DEBUG=green} %logger{-2} - %m%n"
                disableAnsi="false"/>
        </Console>

        <RollingRandomAccessFile
                name="LogToRollingRandomAccessFile"
                fileName="logs/swaglabs_automation.log"
                filePattern="logs/swaglabs-%d{MM-dd-yyyy}-%i.log.gz">

            <PatternLayout>
                <Pattern>%d %p %c{1.} [%t] %m%n</Pattern>
            </PatternLayout>

            <Policies>
                <TimeBasedTriggeringPolicy/>
                <SizeBasedTriggeringPolicy size="10 MB"/>
            </Policies>

            <DefaultRolloverStrategy max="10"/>
        </RollingRandomAccessFile>

        <Async name="Async">
            <AppenderRef ref="LogToRollingRandomAccessFile"/>
        </Async>
    </Appenders>

    <Loggers>

        <!-- 🔴 CRITICAL FIX: Disable HTTP wire logs -->
        <Logger name="org.apache.hc.client5.http.wire" level="OFF" additivity="false"/>
        <Logger name="org.apache.hc.client5.http.headers" level="OFF" additivity="false"/>

        <!-- Reduce Apache HttpClient noise -->
        <Logger name="org.apache.hc.client5" level="WARN"/>
        <Logger name="org.apache.hc.core5" level="WARN"/>

        <!-- Your framework logs -->
        <Logger name="com" level="debug" additivity="false">
            <AppenderRef ref="Async"/>
            <AppenderRef ref="LogToConsole"/>
        </Logger>

        <!-- Root logger -->
        <Root level="error">
            <AppenderRef ref="LogToConsole"/>
        </Root>

    </Loggers>
</Configuration>
